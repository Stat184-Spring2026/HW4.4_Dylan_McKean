# Project and repo plan

## Project Plan

### Goal
Create a PDF file from a QMD file that creates visualizations and answers questions from HW 4.3 in narrative text 

### Needs
busiest airports data set, calcium.csv data set, rvest package, tidyverse package, ggplot2 package, tidy data principles, read_csv(), mutate(), <-, |>, select(), colnames(),
bind_rows(), pivot_longer(), factor(), group_by(), ggplot(), stat_summary(), labs(), theme_minimal()

### Steps
1. Load the data
2. Tidy the data
3. Create visualizations
4. Write narrative text and finalize the QMD
5. Render to PDF

## Repo plan

### Goal
Create and maintain a repo for HW 4.4 that is organized and uses proper version control

### Needs
GitHub repo, main branch, dev branch, commit messages, issues, pull requests, README, PLAN, QMD file, rendered PDF file

### Steps
1. Set up repo
2. Create main and dev branches
3. Create issues to track tasks
4. Commit changes with messages
5. Merge dev into main via pull request
